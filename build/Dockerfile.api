FROM python:3.11-slim

WORKDIR /app

COPY build/requirements-api.txt .

RUN --mount=type=cache,target=/root/.cache/pip \
    pip install -r requirements-api.txt

COPY src/ src/

EXPOSE 8080

ENV PORT=8080

CMD [ "python", "src/api/app.py" ]