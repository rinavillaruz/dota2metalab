# Global settings
global:
  namespace: dota2metalab-dev

# Image settings — GitHub Actions will update this tag with SHA
image:
  tag: 38a473857261bf2724443ecf97850e11fce9bb33

# MongoDB settings
mongodb:
  persistence:
    size: 2Gi
    storageClassName: standard

# Models storage
models:
  persistence:
    size: 500Mi
    storageClassName: standard