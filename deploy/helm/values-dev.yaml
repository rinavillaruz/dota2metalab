# Global settings
global:
  namespace: dota2metalab-dev

# MongoDB settings
mongodb:
  persistence:
    size: 2Gi
    storageClassName: gp2

# Models storage
models:
  persistence:
    size: 500Mi
    storageClassName: gp2